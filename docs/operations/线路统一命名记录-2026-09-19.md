# 线路统一命名记录（2026-09-19）

命名规则：`站点名 · 上游分组名 · #令牌ID`，例如 `nvtokens · 不降智 · #9590`。

- 探针站共有 43 条线路，覆盖 11 个上游站点，全部使用统一名称。
- 按完整 URL、Key 精确核对来源，对应 20 个调度账号；修改请求只提交名称，随后回读核对凭据、分组、并发、状态及推送备注。
- 另有 11 个调度账号未匹配本站探针来源，保留原名称。
- 探针的固定 ID、模型、历史记录和关联关系继续保留。原上游令牌名称保留为元数据，可在令牌管理中查看。
- 探针显示名从持久保存的站点、分组、令牌 ID 生成，每次读取沿用同一规则；后续新推送调度账号也采用该规则。
- 无分组名称时显示分组 ID 或“未分组”。超长名称按 Sub2API 的 100 字节限制缩短站点和分组部分，保留令牌 ID。

完整对照见 [线路统一命名清单](./线路统一命名清单-2026-09-19.csv)。

服务器修改前的加密备份：`/var/backups/signal-monitor/rename-2026-09-19T14-33-30-483Z`，包含账号原始配置、名称清单和逐项改名回读记录。备份含敏感配置，以服务器现有存储密钥加密，未导出凭据到本报告。

检查：22 项后端测试、13 项页面/数据测试通过，覆盖命名、推送去重、关联持久化、探针时间轴、手机布局；构建使用空数据目录。

线上验收：43 条探针名称及 20 个调度账号名称逐项一致；自动调度已恢复开启，无调度错误。桌面与 390px 手机页面无横向溢出或脚本异常。服务状态 active，重启计数为 0。本地 6 个渠道启用探针数仍为 0。
